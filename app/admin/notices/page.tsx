import {ensureStaff} from "@/lib/admin";import {getSupabaseAdmin} from "@/lib/supabase-admin";export const dynamic="force-dynamic";
export default async function P(){await ensureStaff();const {data}=await getSupabaseAdmin().from('notices').select('*').order('is_pinned',{ascending:false}).order('created_at',{ascending:false});return <><section className="card"><h1>공지 관리</h1><form className="form" action="/api/admin/notices" method="post"><input name="title" placeholder="공지 제목" required/><textarea name="content" placeholder="공지 내용" rows={6} required/><label><input name="is_pinned" type="checkbox"/> 상단 고정</label><button className="button primary">공지 등록</button></form></section><section className="card" style={{marginTop:14}}><h2>등록된 공지</h2>{data?.map(n=><article className="notice-item" key={n.id}><form className="form" action={`/api/admin/notices/${n.id}`} method="post"><input name="title" defaultValue={n.title} required/><textarea name="content" defaultValue={n.content} rows={4} required/><label><input name="is_pinned" type="checkbox" defaultChecked={n.is_pinned}/> 상단 고정</label><div className="actions"><button className="button">수정 저장</button><button className="button danger" name="_action" value="delete">삭제</button></div></form></article>)}</section></>}
