import { requireStaff } from "@/lib/session";import { getSupabaseAdmin } from "@/lib/supabase-admin";import TournamentManager from "./TournamentManager";export const dynamic="force-dynamic";export default async function Page(){await requireStaff();const db=getSupabaseAdmin();const [{data:events},{data:teams},{data:players},{data:matches}]=await Promise.all([db.from("tournaments").select("*").order("created_at",{ascending:false}),db.from("tournament_teams").select("*").order("seed"),db.from("tournament_players").select("*").order("line_order"),db.from("tournament_matches").select("*").order("round").order("slot")]);return <TournamentManager events={(events||[]) as never[]} teams={(teams||[]) as never[]} players={(players||[]) as never[]} matches={(matches||[]) as never[]}/>}
